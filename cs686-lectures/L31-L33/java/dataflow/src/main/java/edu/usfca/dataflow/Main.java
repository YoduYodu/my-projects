package edu.usfca.dataflow;

import java.io.IOException;

public class Main {

  public static void main(String[] args) throws IOException {
    // Execute one line at a time to avoid unexpected behaviors.

    // L31Storage.execute(args);

    // L32PubSub.execute(args, 1);
    // L32PubSub.execute(args, 2);
    // L32PubSub.execute(args, 3);

    // L33BigQuery.execute(args);
  }
}

